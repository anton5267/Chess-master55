@page
@model LockoutModel
@{
    ViewData["Title"] = Localizer["Id_LockoutTitle"];
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">@Localizer["Id_LockoutMessage"]</p>
</header>
